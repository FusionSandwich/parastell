name: parastell_env
channels:
  - conda-forge
dependencies:
  - cadquery
  - gmsh
  - moab>=5.5.1[build=nompi_tempest_*]
  - cad_to_dagmc>=0.9.1
  - openmc>=0.15.1[build=dagmc_nompi_*]
  - h5py
  - matplotlib
  - numpy
  - scipy
  - pip
  - pip:
    - netcdf4
    - pyyaml
    - pydagmc
    - pytest
    - pytest-cov
    - build
    - black==25.*
