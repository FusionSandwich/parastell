# Prompt-1 missing or unqualified inputs

This is a fail-closed preflight inventory, not a claim that all listed items block source and test implementation.

1. **The producer image still has no default depletion-chain binding.** OpenMC 0.16.0 exposes `R2SManager`, `IndependentOperator`, and `get_microxs_and_flux`, and the mounted transport library has 631 neutron plus 97 photon entries. The unchanged image therefore classifies as `MISSING_CHAIN_OR_DATA` until a chain is explicitly bound. A read-only binding of the local 3,820-nuclide chain at `D:\openc-hts-dpa-data\runs\wistell_d_openmc_to_atomistics_v2\nuclear_data\pilot_chain.xml` (SHA-256 `5eeb727498d824d7c951ad89864bbc1c2d76ec5e8c9097a820505213ba6a2bf3`) passed an analytic OpenMC 0.16 R2S/IndependentOperator/shutdown-photon smoke without changing the image.
2. **The real 5,000-history pilot is statistically under-resolved and is not activation-qualified.** It has 34 neutron and 2 photon boundary records, but zero observed incoming photons and 13 of 18 banks empty. Every nonzero whole-volume scalar-flux bin is under-resolved. The local meshes are spatially mixed, have no qualifying cell/material filter or material-intersection volumes, and predate the activation-ready metadata contract. Empty banks are insufficient observations, not physical zero. This pilot qualifies schema and reader plumbing only; no real-pilot R2S or IndependentOperator result is claimed.
3. **No production response-convergence evidence exists for all SourceMesh candidates.** The finest declared source grid remains an unqualified conservative fallback.
4. **No stable qualified weight-window mesh exists.** The prior HXT attempt was rejected for intersecting PLC facets; unbiased transport remains mandatory.
5. **Real-pilot local-mesh material-intersection volumes are not present.** The historical regular meshes record geometric bins only and do not have a qualifying cell/material filter. New producer metadata distinguishes geometric from material-intersection volume and keeps such meshes nondepletable until nonoverlap and component ownership are proven.
6. **The TENDL matrix archive is present, but processing provenance remains incomplete.** The 13 required YBCO isotope files were verified on the common 660-group grid. However, the processing audit still lacks complete provenance, contains an empty helium placeholder, and has unresolved MT5/light-ion contribution semantics. The prior real SPECTRA-PKA package is therefore evidence, not production qualification.
7. **No high-statistics production run has been launched.** History planning must be derived from the measured pilot crossing rates and effective-sample targets first.
8. **Shutdown photons are qualified only for the analytic Cu-63 API smoke.** The bound-chain run generated a Cu decay-photon source and a nonzero shutdown-photon tally. This demonstrates the OpenMC software path, not the real pilot, all requested materials, or a production source. Product-specific photon spectra remain missing for some requested-material activation products and must be reported rather than zero-filled.
9. **The full-chain 14.1 MeV analytic attempt is data-range blocked.** Loading all chain nuclides/reactions with a 14.1 MeV source reached at least one transport table whose maximum neutron energy is below the source. The passing retry deliberately used a 0.0253 eV Cu-63 capture source. Fusion-spectrum activation therefore requires an explicit material/reaction subset whose neutron tables are individually verified across the requested energy range; the thermal smoke must not be generalized to 14.1 MeV readiness.

No item above justifies silently replacing missing reaction rates by zero, using surface current as scalar flux, enabling weight windows, adding ports, or launching production transport.
