+            tally_direction="incoming",
+            record_direction="outgoing",
+        )
+
+    with pytest.raises(ValueError, match="explicit tally and record"):
+        replay_phase_space(
+            phase,
+            spectra,
+            tmp_path / "all-directions.h5",
+            stack=stack,
+            response_library=library,
+            tally_direction=None,
+            record_direction=None,
+        )
+
+
+def test_direction_selection_requires_phase_space_labels(tmp_path):
+    phase = tmp_path / "phase.h5"
+    spectra = tmp_path / "spectra.h5"
+    _write_phase_space(phase)
+    _write_spectra(spectra)
+    with h5py.File(phase, "r+") as source:
+        del source["phase_space"]["magnet_direction"]
+    stack = verification_rebco_stack()
+    library = zero_response_library(
+        stack,
+        [0.0, 20.0e6],
+        ["neutron"],
+    )
+
+    with pytest.raises(ValueError, match="phase space has no"):
+        replay_phase_space(
+            phase,
+            spectra,
+            tmp_path / "replay.h5",
+            stack=stack,
+            response_library=library,
+        )
